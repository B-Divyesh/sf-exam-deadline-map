{
  "name": "exam-deadline-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/write-static-config.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "audit:browser": "node scripts/browser-audit.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@types/node": "^22.15.3",
    "lighthouse": "^13.4.1",
    "playwright": "^1.62.1",
    "typescript": "^5.8.3",
    "vite": "^6.1.0",
    "vitest": "^3.1.1"
  }
}
